# Session 管理界面

Session 管理列表会根据可用宽度自动调整布局：

- 桌面端使用紧凑列宽，账号、激活码和执行时间过长时以省略号显示。
- 宽度不足时自动切换为分组信息卡片，不需要横向滑动即可看到复制、导出、续费和删除操作。
- 点击 Session 预览仍可打开完整内容，表格中的数据和操作不受布局调整影响。

其他后台数据列表（代理池、地址池、银行卡、Orbitcard、任务、待人工确认和账单）统一将最右侧操作列固定在可视区域右侧。数据列仍可横向查看，但检测、恢复、删除、解除等操作不再随着内容滚出屏幕。
